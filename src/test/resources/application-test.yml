backend:
  accounts:
    - name: "Тестовый 1"
      description: "Необходимо для тестирования"
      init-amount: 5000
      type: default

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSSXXX} %clr(%5level) [%15.15t] %clr(%-45.55logger{39}){cyan} : %m%n%wEx"
  level:
    ru.gpbitfactory.minibank.middle:
      createclient:
        service: debug
        restclient: debug
      createaccount:
        service: debug
        restclient: debug